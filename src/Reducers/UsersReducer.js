

// to fill in properly in a bit
const initialState = {
    // user: null,
    // loggedIn: false,
    // authenticated: false,
    // loginSuccess: false,
    // error: null

}


const usersReducer = (state=initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default usersReducer